/**
 * `@p2pdotme/widgets/payment-history` — read-only widget that lists the
 * connected user's orders from the subgraph, with resume support for
 * pending ones.
 *
 * Shared types (CheckoutSigner, P2PTheme, OrderStatus, …) live on the bare
 * `@p2pdotme/widgets` import.
 */

export { PaymentHistory, type PaymentHistoryProps } from "./widgets/PaymentHistory";
